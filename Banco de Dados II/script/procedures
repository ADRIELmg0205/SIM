create or replace function add_estoque(qtd int )
returns int as
$body$
begin
	return qtdest+ qtd;
end
$body$
language plpgsql;

create or replace function add_vend(vend int )
returns int as
$body$
begin
	return vend + vlrven;
end
$body$
language plpgsql;
